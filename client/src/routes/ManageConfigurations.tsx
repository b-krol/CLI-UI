import ConfigurationsTable from "../components/ConfigurationsTable";

export default function ManageConfigurations(){
    return(
        <ConfigurationsTable data={[]}/>//TODO paste in configurations from API into 'data' property
    )
}